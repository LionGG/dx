# A股数据仓库定时任务 - 最终确认版（2026-02-26）

## 重要提示
本文档为最终确认版本，明天（2026-02-27）开始严格执行。
**禁止擅自修改、禁止用旧版本、禁止自己发明新方法。**

---

## 今天纠正的细节问题（必须记住）

### 1. 创业板指MA50计算
**问题**: 切换创业板指K线时，50日均线没有数据
**原因**: 新插入的数据没有计算MA50
**解决**: 获取K线数据时，确保计算最近50天的MA50并更新

### 2. HTML字段索引错位
**问题**: 周期定位、操作方向、一句话总结、墨问链接错位
**原因**: SQL查询字段索引对应错误
**解决**: 
- row[14]: analysis_phase (周期定位)
- row[15]: analysis_action (操作方向)
- row[16]: analysis_summary (一句话)
- row[17]: mowen_link (链接)
- 条件判断: if row[14]: (有AI分析)

### 3. 日历组件位置
**问题**: 日历切换组件在卡片内部
**解决**: 移到"情绪研判"标题上方

### 4. AI分析prompt
**问题**: 一开始使用了简化版格式
**解决**: 必须使用确认好的prompt.md格式，三部分结构

### 5. 近5日演变描述
**问题**: 描述太复杂，每天列出
**解决**: 概括描述，不每天列出

---

## 任务1：个股数据抓取 - 16:05

**脚本**: `stock/a-share-warehouse/scripts/sync_stock_from_mysql_v2.py`

**执行逻辑**:
1. 判断今天是否为交易日（使用trading_calendar表）
2. 连接MySQL (82.156.239.131:3306)
3. 检查MySQL是否有今天数据
4. 同步到SQLite
5. 输出结果

**推送**: OpenClaw捕获控制台输出 → 个人飞书

**输出格式**:
```
✅ 个股数据抓取-16:05 执行成功 - 2026-02-27

同步结果:
- 今天(2026-02-27): X条 ✅
- 昨天(2026-02-26): Y条

说明: 数据同步完成，今天数据已更新。（可能有停牌或退市的股票）
```

---

## 任务2：MA50占比计算 - 16:10

**脚本**: `stock/a-share-warehouse/scripts/calculate_ma50_ratio.py`

**执行逻辑**:
1. 检查今天是否有MA50数据
2. 如果没有，计算今天的MA50（基于stock_kline）
3. 生成趋势图
4. **发送文字+图片到飞书群**（调用send-to-feishu-group.py）
5. 输出结果

**推送**:
- 飞书群：文字+图片（调用外部脚本）
- 个人：OpenClaw捕获输出

**依赖**: 个股数据抓取-16:05（必须先有数据）

---

## 任务3：短线情绪研判 - 16:20

**项目目录**: `/root/.openclaw/workspace/stock/dx/`

**执行步骤**:
1. **抓取短线侠情绪数据** - `python3 scripts/crawler.py`
2. **抓取AKShare K线数据** - `python3 scripts/fetch_kline_akshare.py`
3. **同步MA50占比数据** - `python3 scripts/sync_ma50_ratio.py`
4. **AI分析** - 读取最近5天数据，使用prompt.md格式，AI大脑直接分析
5. **发布到墨问** - `python3 scripts/publish_to_mowen.py`
6. **生成HTML** - `python3 scripts/update_html_data.py`
7. **部署到GitHub** - `python3 scripts/deploy.py`
8. **发送飞书群通知** - 文字（周期定位+墨问链接）

**AI分析要点**:
- 读取最近5个交易日数据
- 使用 `stock/dx/prompt.md` 确认好的格式
- 三部分结构：当日盘面解读、周期定位与演变、后市策略应对
- AI大脑直接分析，**不调用API**

**飞书群通知内容**:
```
📊 短线情绪研判 - [日期]

周期定位：[analysis_phase]

查看完整分析：[墨问链接]
```

**注**：图片暂时不发（HTML截图工具待解决）

**推送**: OpenClaw捕获输出 → 个人飞书

---

## 关键文件清单

| 文件 | 用途 | 状态 |
|------|------|------|
| `sync_stock_from_mysql_v2.py` | 个股数据同步 | ✅ 使用 |
| `calculate_ma50_ratio.py` | MA50计算+群推送 | ✅ 使用 |
| `sync_ma50_ratio.py` | MA50同步到duanxian | ✅ 使用 |
| `crawler.py` | 短线侠数据 | ✅ 使用 |
| `fetch_kline_akshare.py` | 指数K线 | ✅ 使用 |
| `publish_to_mowen.py` | 发布到墨问 | ✅ 使用 |
| `update_html_data.py` | 生成HTML | ✅ 使用 |
| `deploy.py` | GitHub部署 | ✅ 使用 |
| `prompt.md` | AI分析模板 | ✅ 使用 |
| ~~archive/*~~ | 旧版本 | ❌ 已删除 |
| ~~API密钥版本~~ | 废弃 | ❌ 已废弃 |

---

## 禁用任务（数据源不稳定）

- ❌ 指数数据抓取
- ❌ 板块数据抓取
- ❌ 涨停池抓取
- ❌ 龙虎榜资金流向
- ❌ 板块成分股更新

---

## 明天执行检查清单

- [ ] 读取本文档确认流程
- [ ] 16:05 执行个股数据抓取
- [ ] 16:10 执行MA50占比计算
- [ ] 16:20 执行短线情绪研判（7步骤）
- [ ] 每步检查输出结果
- [ ] 最终确认飞书推送

---

**记录时间**: 2026-02-26 18:15
**执行日期**: 2026-02-27开始
**版本**: FINAL v1.0
