# 2026-02-25 对话归档

## 主题
邮件服务配置 + 飞书交易通知机器人配置

---

## 一、邮件服务配置

### 已完成
- 安装 msmtp 邮件发送工具
- 配置 QQ 邮箱 SMTP（smtp.qq.com:587）
- 授权码：`chsiheefifwpcaea`
- 创建发送脚本：`/root/.openclaw/workspace/send-email.sh`

### 使用方法
```bash
# 发送纯文本邮件
./send-email.sh "收件人邮箱" "主题" "内容"

# 发送带附件
./send-email.sh "收件人邮箱" "主题" "内容" "/path/to/file"

# 示例
./send-email.sh "113314650@qq.com" "测试" "邮件内容"
```

### 配置文件
- `/etc/msmtprc` - QQ 邮箱 SMTP 配置
- `/var/log/msmtp.log` - 邮件发送日志

---

## 二、飞书交易通知机器人配置

### 方案选择
| 方案 | 说明 | 状态 |
|------|------|------|
| A | 本机+云服务器反向代理 | 备选 |
| B | 全云服务器（自建应用） | 备选 |
| **C** | **全云服务器（群机器人）** | **已选定** |

### 群机器人配置步骤

#### 1. 创建群机器人
- 进入飞书群 → 群设置 → 群机器人 → 添加机器人
- 选择"自定义机器人"
- 复制 Webhook 地址（格式：`https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxx`）

#### 2. 云服务器部署
```bash
# 安装依赖
apt install -y python3 python3-pip nginx
pip3 install flask requests gunicorn

# 创建服务目录
mkdir -p /opt/webhook
```

#### 3. Webhook 服务代码
文件：`/opt/webhook/app.py`
```python
from flask import Flask, request
import requests

app = Flask(__name__)

# 修改这里：填入你的群机器人 Webhook 地址
FEISHU_WEBHOOK = "https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxxxxxxxxxxxx"

def send_feishu(text):
    requests.post(FEISHU_WEBHOOK, json={"msg_type": "text", "content": {"text": text}})

@app.route('/webhook/trade', methods=['POST'])
def trade():
    data = request.json
    emoji = "🟢" if data.get('action') == 'buy' else "🔴"
    msg = f"{emoji} 交易提醒\n\n股票: {data.get('stock_name')}({data.get('stock_code')})\n操作: {data.get('action').upper()}\n价格: ¥{data.get('price')}\n数量: {data.get('quantity')}"
    send_feishu(msg)
    return {"success": True}

if __name__ == '__main__':
    app.run(host='127.0.0.1', port=5000)
```

#### 4. 启动服务
```bash
cd /opt/webhook
nohup python3 app.py > app.log 2>&1 &
```

#### 5. Nginx 配置
```nginx
server {
    listen 80;
    server_name webhook.yourdomain.com;
    location / {
        proxy_pass http://127.0.0.1:5000;
    }
}
```

#### 6. HTTPS 证书
```bash
apt install -y certbot python3-certbot-nginx
certbot --nginx -d webhook.yourdomain.com
```

### 调用示例
```bash
curl -X POST https://webhook.yourdomain.com/webhook/trade \
  -H "Content-Type: application/json" \
  -d '{
    "action": "buy",
    "stock_code": "601888",
    "stock_name": "中国中免",
    "price": 85.50,
    "quantity": 100
  }'
```

### Python 接入
```python
import requests

WEBHOOK_URL = "https://webhook.yourdomain.com/webhook/trade"

def notify(action, code, name, price, qty):
    requests.post(WEBHOOK_URL, json={
        "action": action,
        "stock_code": code,
        "stock_name": name,
        "price": price,
        "quantity": qty
    })

# 使用
notify("buy", "601888", "中国中免", 85.50, 100)
```

---

## 三、相关文档

| 文档 | 路径 | 说明 |
|------|------|------|
| 极简部署指南 | `/root/.openclaw/workspace/webhook-simple-guide.md` | 群机器人方案 |
| 云服务器独立部署 | `/root/.openclaw/workspace/webhook-cloud-deployment-guide.md` | 完整方案（含自建应用）|
| 本机+云服务器方案 | `/root/.openclaw/workspace/webhook-deployment-guide.md` | 含 frp 穿透 |

---

## 四、待办事项

- [ ] 用户按文档在云服务器部署 webhook 服务
- [ ] 配置域名和 HTTPS 证书
- [ ] 测试交易通知推送
- [ ] 接入交易系统

---

## 五、本机消息转发到群功能

### 触发词
- "推送到群里"
- "转发到群"
- "发到群里"
- "群里也发一份"

### 使用方法
```
用户：把这条消息推送到群里
→ 自动转发当前对话内容到飞书群

用户：把早上的交易计划推送到群里
→ 自动查找并转发交易计划到群

用户：定时任务结果也发到群里
→ 定时任务自动推送到群
```

### 配置文件
- `/root/.openclaw/workspace/feishu_group_sender.py`
- Webhook 地址已配置

---

## 六、关键信息

- 用户邮箱：113314650@qq.com
- 飞书 App ID：cli_a91e22f1e138dbef
- 飞书群 Webhook：https://open.feishu.cn/open-apis/bot/v2/hook/c21f215d-e186-4b3b-bb50-1ebbe4e994df
- QQ 邮箱授权码：chsiheefifwpcaea
- 邮件发送脚本：/root/.openclaw/workspace/send-email.sh
- 群消息转发脚本：/root/.openclaw/workspace/feishu_group_sender.py

---

归档时间：2026-02-25 12:44
