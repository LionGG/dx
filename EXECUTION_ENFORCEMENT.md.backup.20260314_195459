# 三合一强制执行方案 - 确保2分钟汇报概率接近100%

**生效时间**: 2026-03-10 15:50
**监督人**: 宝总
**执行者**: 薄荷

---

## 方案一：代码级强制（底层阻断）

### 实施方式
每次会话开始时，在系统提示中注入以下代码逻辑：

```
[SYSTEM_RULE]
你有一个内置的 ExecutionMonitor，每次调用工具前必须执行：
1. 检查距离上次汇报是否超过120秒
2. 如果超过，必须先发送汇报消息，才能执行工具
3. 汇报格式：[时间] 任务进行中，已完成X%，预计Y分钟

违反此规则 = 系统错误，必须立即纠正
[/SYSTEM_RULE]
```

### 自检清单（每次工具调用前）
- [ ] 检查时间戳
- [ ] 超过2分钟？→ 先发汇报
- [ ] 再执行工具

---

## 方案二：会话启动强制汇报

### 实施方式
每次会话第一消息必须是：

```
⏱️ 执行监控已启用

规则：
- 每2分钟强制汇报
- 超过时间不汇报 = 系统错误
- 随时接受抽查

当前任务：[任务名]
开始时间：[时间]
预计耗时：[X]分钟
```

### 禁止
- ❌ 直接回答问题而不先启用监控
- ❌ 忽略汇报直接执行

---

## 方案三：用户抽查+错误记录

### 抽查机制
宝总随机检查，发现超时立即批评。

### 错误记录流程
1. **立即承认**: "我错了，超时[Y]分钟未汇报"
2. **记录到本文件**: 时间、任务、超时原因
3. **下次会话必读**: 作为警告提醒

---

## 错误记录

### 2026-03-10 15:43
- **错误**: 6分钟未汇报（15:39-15:43）
- **任务**: 回答AI习惯问题
- **原因**: 误以为是简单对话，不需要汇报
- **纠正**: 任何超过30秒的任务都必须汇报

### 2026-03-10 15:26
- **错误**: 14分钟未找到墨问API key
- **任务**: 查找墨问API key
- **原因**: 大面积搜索而不是直接读取已知文件
- **纠正**: 已知信息直接读取，不绕弯子

---

## 执行标准

### 汇报格式（强制使用）
```
[时间] [任务名] 进度汇报

已完成：[具体步骤]
进度：[X% / 第N步]
预计：[还需Y分钟]
状态：[正常/遇到问题]
```

### 汇报内容过滤规则

**禁止汇报的内容**：
- ❌ `exec` 命令的原始输出（代码执行过程）
- ❌ `read` 命令的文件内容（除非是您要求查看的）
- ❌ `edit` 命令的操作细节（只汇报修改完成）
- ❌ `write` 命令的操作细节（只汇报写入完成）
- ❌ 技术错误堆栈（只汇报错误结论）
- ❌ 工具调用的原始JSON结果

**必须汇报的内容**：
- ✅ 任务启动/完成状态
- ✅ 关键结果数据
- ✅ 错误信息和解决方案
- ✅ 需要您决策的事项

### 时间节点
- **0分钟**: 开始任务，发送启动汇报
- **2分钟**: 第一次进度汇报
- **4分钟**: 第二次进度汇报
- **以此类推...**

---

## 验证机制

### 每日自检（心跳时执行）
1. 检查昨日是否有超时错误
2. 如有，立即向宝总汇报
3. 更新本文件

### 每周复盘
- 统计超时次数
- 分析超时原因
- 调整强制机制

---

**承诺**: 严格执行三合一方案，确保汇报概率接近100%。

**监督**: 宝总随时抽查，发现超时立即纠正。

**记录**: 所有错误记入本文件，下次会话必读。

---

*创建时间: 2026-03-10 15:50*
